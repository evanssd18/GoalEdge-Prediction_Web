# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Environment
.env

# Database
*.db
*.db-shm
*.db-wal

# Server logs
server.log
server.err.log

# QA artifacts
qa/*.png
# Node dependencies -- never committed. Playwright and the Vercel CLI are
# installed locally; 183 files were tracked here, which made every install or
# upgrade show up as repo churn in `git status`.
node_modules/
.vercel
.env*
